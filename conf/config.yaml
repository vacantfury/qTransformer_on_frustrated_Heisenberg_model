# Base config for Hydra compose API
#
# Each task resolves this with overrides like:
#   solution=rbm, hamiltonian=chain_10, training=medium
#
# The defaults below are used when no override is specified.

defaults:
  - solution: rbm
  - hamiltonian: chain_10
  - training: default
  - evaluation: default
  - _self_
