# Production training config (extended runs for final results)
n_steps: 2000
learning_rate: 0.005
n_samples: 2048
n_chains: 1
n_discard_per_chain: 16
d_max: 2

sr:
  diag_shift: 0.01

log_every: 20
checkpoint_every: 200
early_stop_variance: 1.0e-7
early_stop_patience: 100
early_stop_min_steps: 500
